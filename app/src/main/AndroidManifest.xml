<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mobieapphnd">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.MobieAPPHND">

        <activity
            android:name=".ui.login.MainActivity"
            android:exported="true"
            android:theme="@style/Theme.MobieAPPHND">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.login.LoginActivity"
            android:exported="false"
            android:theme="@style/Theme.MobieAPPHND" />

        <activity
            android:name=".ui.login.RegisterActivity"
            android:exported="false"
            android:theme="@style/Theme.MobieAPPHND" />

        <activity
            android:name=".ui.login.HomeActivity"
            android:exported="false" />

        <activity
            android:name=".ui.login.ProductDetailActivity"
            android:exported="false"
            android:parentActivityName=".ui.login.HomeActivity" />

    </application>

</manifest>